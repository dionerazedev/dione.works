import { ArrowUpRight } from '@phosphor-icons/react';
import { EXPERIENCE } from '../../data/experience';
import { PROFILE_LINKS } from '../../data/navigation';
import { CREDENTIALS } from '../../data/technologies';
import { ProjectImage } from '../ui/ProjectImage';
import { SectionHeading } from '../ui/SectionHeading';

export const ExperienceSection = () => <section id="experience" className="section-shell experience-section" aria-labelledby="experience-heading"><SectionHeading number="04" label="experience" id="experience-heading" title="Independent product development across web, mobile, and AI systems." comment="Current contractor work focused on shipping practical full-stack products and business tools." /><div className="experience-layout"><div className="experience-list">{EXPERIENCE.map((entry) => <article key={entry.role}><time>{entry.period}</time><div><h3>{entry.role}</h3><p className="experience-engagement">{entry.engagement}</p>{entry.summary.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}{entry.source && <p className="experience-source">{entry.source}</p>}<div className="tag-row">{entry.technologies.map((technology) => <span key={technology}>{technology}</span>)}</div></div></article>)}<a href={PROFILE_LINKS.resume} target="_blank" rel="noreferrer" className="text-link">Full résumé <ArrowUpRight size={12} /></a></div><section id="certifications" className="certification-list" aria-labelledby="certifications-heading"><header><p className="page-label">Certifications</p><h3 id="certifications-heading">Verified credentials</h3></header>{CREDENTIALS.map((credential) => <article key={credential.title}><ProjectImage src={credential.image} alt={credential.alt} width={credential.width} height={credential.height} /><div><span>{credential.issuer}{credential.achieved ? ` · ${credential.achieved}` : ''}</span><h4>{credential.title}</h4></div></article>)}</section></div></section>;
